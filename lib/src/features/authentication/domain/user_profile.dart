import 'app_user.dart';

class UserProfile {
  // Base user data (from AppUser)
  final AppUser appUser;

  // Specific profile data
  // TODO: add specific profile information

  const UserProfile({
    required this.appUser,
  });
}
